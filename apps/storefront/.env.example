# See root .env.example for Shopify vars.
#
# Production storefront URL (this project): https://oscarslab.dev
# — After deploy, set VITE_SHOPIFY_REDIRECT_BASE only if OAuth must use a fixed host; else omit (uses current origin).

# Optional: same value as root BACKEND_API_KEY — Vite dev proxy adds X-API-Key to /api requests
# VITE_BACKEND_API_KEY=
